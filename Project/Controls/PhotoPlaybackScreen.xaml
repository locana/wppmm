<UserControl x:Class="Kazyx.WPPMM.Controls.PhotoPlaybackScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="640" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Image Name="Image" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="12,36,12,12" Source="{Binding Image}"/>
        <ScrollViewer Name="DetailInfoPanel" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="12">
            <Grid Name="DetailInfoGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="6*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" Text="ISO" />
                <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding IsoValue}" />
                <TextBlock Grid.Column="0" Grid.Row="1" Text="F number" />
                <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding FValue}" />
                <TextBlock Grid.Column="0" Grid.Row="2" Text="Shutter speed" />
                <TextBlock Grid.Column="1" Grid.Row="2" Text="{Binding ShutterSpeedValue}" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>