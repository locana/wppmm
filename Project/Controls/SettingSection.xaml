<UserControl x:Class="Kazyx.WPMMM.Controls.SettingSection"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="-10,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Border Background="{StaticResource PhoneChromeBrush}" Margin="0,5" >
            <TextBlock Name="TitleTextBlock" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" FontSize="{StaticResource PhoneFontSizeMediumLarge}" 
                   Foreground="{StaticResource PhoneAccentBrush}" Text="" Margin="25,5" />
        </Border>
        <StackPanel Name="SettingItems" Grid.Row="1"></StackPanel>
    </Grid>
</UserControl>